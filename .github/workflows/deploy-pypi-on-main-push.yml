name: Deploy PyPI on Main Push

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install Poetry and Twine
        run: |
          pip install poetry twine

      - name: Bump version, build and upload to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          set -e

          echo "--- 기존 dist/ 삭제 ---"
          rm -rf dist

          echo "--- Git 설정 ---"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          echo "--- 버전 패치 (poetry version patch) ---"
          poetry version patch

          echo "--- 변경사항 커밋 ---"
          git add pyproject.toml
          git commit -m "🔖 Bump version for PyPI release [skip ci]" || echo "No version change to commit"

          echo "--- 커밋 푸시 ---"
          git push origin HEAD

          echo "--- 빌드 ---"
          poetry build

          echo "--- PyPI 업로드 ---"
          twine upload --repository pypi dist/*

          echo "--- ✅ 배포 완료 ---"
